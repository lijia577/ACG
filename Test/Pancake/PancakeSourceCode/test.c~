#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int *max(int *x, int *y){
	if(*x>*y) return x;
	return y;
}

int main(){
int a=10, b=20;

int *res=max(&a,&b);
printf("%d\n",*res);

return 0;
}
