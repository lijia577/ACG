#include <stdio.h>
#include <stdlib.h>
#include <math.h>

//This is to practice getting arguments from the command line.


int main ( int argc ){

    if ( argc != 2 ){ // only if argc ==2 we proceed.
        //printf( "usage: %s filename", argv);
    }else {
        // We assume argv[1] is a filename to open
        FILE *file = fopen("df", "r" );

        /* fopen returns 0, the NULL pointer, on failure */
        if ( file == 0 ){
            printf( "Could not open file\n" );
        }
        else {
        }
        
        //fclose(file);
    }
}
